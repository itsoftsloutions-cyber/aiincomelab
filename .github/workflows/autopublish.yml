name: Auto-publish & Deploy

# Publishes a new post every 2 hours and deploys to GitHub Pages — fully free.
on:
  schedule:
    - cron: "7 */2 * * *"   # every 2 hours, offset 7 min to avoid the :00 rush
  workflow_dispatch: {}
  push:
    branches: [master, main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      # Only generate a new post on scheduled runs, then always rebuild.
      - name: Publish next queued topic
        if: github.event_name == 'schedule'
        run: node new-post.js

      - name: Build site
        run: node build.js

      - name: Commit new content
        if: github.event_name == 'schedule'
        run: |
          git config user.name  "aiincomelab-bot"
          git config user.email "bot@users.noreply.github.com"
          git add content/posts data/state.json
          git commit -m "auto: publish next queued post" || echo "nothing to commit"
          git push || echo "nothing to push"

      # Share the newly published post to all social platforms. No-ops cleanly
      # for any platform whose secrets are not configured, and never fails the build.
      - name: Distribute to social media
        if: github.event_name == 'schedule'
        continue-on-error: true
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          LINKEDIN_ACCESS_TOKEN: ${{ secrets.LINKEDIN_ACCESS_TOKEN }}
          LINKEDIN_AUTHOR_URN: ${{ secrets.LINKEDIN_AUTHOR_URN }}
          FACEBOOK_PAGE_ID: ${{ secrets.FACEBOOK_PAGE_ID }}
          FACEBOOK_PAGE_TOKEN: ${{ secrets.FACEBOOK_PAGE_TOKEN }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: |
          node scripts/social-distribute.js
          git add data/social-log.json || true
          git commit -m "auto: log social distribution" || echo "nothing to commit"
          git push || echo "nothing to push"

      - uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    needs: publish
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
